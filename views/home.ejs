<%- include("partials/header"); -%>

<h1>Home</h1>
<p> <%= homecontent %> </p>


<!-- Using for each and for loops
posts.forEach(post=>{
  <h3> post.title </h3>
  <p> post.content </p>
 });

  for(var i=0; i<posts.length; i++){
   <h3> posts[i].title </h3>
   <p> posts[i].content </p>
  };
-->

<% posts.forEach(function(post){ %>
  <h3><%= post.title %></h3>
  <p><%= post.content.substr(0, 100) + "...." %> <a href="/posts/<%= post._id %>">Read More</a> </p>
<% }); %>



<%- include("partials/footer"); -%>
